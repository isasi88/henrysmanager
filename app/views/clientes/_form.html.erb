<div class="form-new wide-form">
<%= form_for [@user,@cliente] do |f| %>
  <% if @cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% @cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<h1 class="text-center main-h1">Nuevo cliente</h1>
  <div class="form-group">
    <%= f.label :name, "Nombre de cliente" %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :telephone, "Telefono de cliente" %><br>
    <%= f.text_field :telephone, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :email, "Email del cliente" %><br>
    <%= f.text_field :email, class: "form-control" %>
  </div>
  <div class="form-actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
