<p id="notice"><%= notice %></p>
<h3> Notas para tu reunión con <%= @meeting.title %> </h3>

<%= form_for [current_user,@meeting] do |f| %>
	</br>
	<%= f.submit "Guardar Notas" %>
	</br>
	</br>
	<%= f.text_area :note, class: "editor" %>
	</br>
	<%= f.submit "Guardar Notas" %>
<% end %>
 